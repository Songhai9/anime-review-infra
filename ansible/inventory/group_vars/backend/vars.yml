backend_container_name: anime-review-api
backend_port: "3001"

backend_image: "{{ registry_image_prefix }}/backend:{{ image_tag }}"

database_private_ip: "{{ groups['database'][0] }}"
db_port: "5432"
